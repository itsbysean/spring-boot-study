spring:
  profiles:
    active: kor
  application:
    name: microservice
  cloud:
    config:
      uri: http://groupadd-config:8888
eureka:
  client:
    service-url:
      defaultZone: http://groupadd-eureka:8761/eureka
